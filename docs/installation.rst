============
Installation
============

At the command line::

    $ easy_install localhostPyVisor

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv localhostPyVisor
    $ pip install localhostPyVisor